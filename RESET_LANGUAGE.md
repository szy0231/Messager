# Reset Language Selection

To see the language selection modal again, open your browser console (F12) and run:

```javascript
localStorage.removeItem('messager-language');
location.reload();
```

This will clear your language preference and show the language selection screen again.

